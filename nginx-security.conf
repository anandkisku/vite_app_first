# Security headers
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "no-referrer-when-downgrade" always;
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' ws: wss:;" always;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

# Disable server signature
server_tokens off;

# The location directives need to be in a server block, which we'll include in the main nginx.conf
# These lines are commented out since they were causing errors
# location ~ /\. {
#     deny all;
#     access_log off;
#     log_not_found off;
# }

# location * (?:\.(?:bak|conf|dist|fla|in[ci]|log|psd|sh|sql|sw[op])|)$ {
#     deny all;
#     access_log off;
#     log_not_found off;
# }